

export class BankAccount {
    private _accountOwner: string
    private _balance: number

    get accountOwner(): string {
        return this._accountOwner
    }
    get balance(): number {
        return this._balance
    }

    constructor(accountOwner: string, balance: number) {
        if()
    }
}